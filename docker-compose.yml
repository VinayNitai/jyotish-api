version: '3.8'

services:
  # Serviço PHP
  php:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jyotish-api-php

  # Serviço Nginx (sem healthcheck)
  nginx:
    # Voltamos a usar a imagem padrão do Nginx
    image: nginx:alpine
    container_name: jyotish-api-nginx
    ports:
      - "80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/jyotish.conf
      - ./api:/var/www/html
    depends_on:
      - php
